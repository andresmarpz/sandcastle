appId: com.sandcastle.desktop
productName: Sandcastle
directories:
  buildResources: build
files:
  - '!**/.vscode/*'
  - '!src/*'
  - '!electron.vite.config.{js,ts,mjs,cjs}'
  - '!{biome.json,dev-app-update.yml,CHANGELOG.md,README.md}'
  - '!{.env,.env.*,.npmrc,pnpm-lock.yaml}'
  - '!{tsconfig.json,tsconfig.node.json,tsconfig.web.json}'
asarUnpack:
  - resources/**
  - "**/node_modules/node-pty/**"
# The bundled abduco binaries (PTY persistence, see docs/pty-persistence.md §7) are
# not imported by vite, so they are not in the app bundle. Copy them verbatim into
# the packaged app's Resources/bin so src/main/abduco.ts can resolve them at
# join(process.resourcesPath, "bin", "abduco-<platform>-<arch>").
extraResources:
  - from: resources/bin
    to: bin
    filter:
      - "abduco-*"
win:
  executableName: Sandcastle
nsis:
  artifactName: ${name}-${version}-setup-${arch}.${ext}
  shortcutName: ${productName}
  uninstallDisplayName: ${productName}
  createDesktopShortcut: always
mac:
  entitlementsInherit: build/entitlements.mac.plist
  extendInfo:
    - NSCameraUsageDescription: Application requests access to the device's camera.
    - NSMicrophoneUsageDescription: Application requests access to the device's microphone.
    - NSDocumentsFolderUsageDescription: Application requests access to the user's Documents folder.
    - NSDownloadsFolderUsageDescription: Application requests access to the user's Downloads folder.
  notarize: false
dmg:
  artifactName: ${name}-${version}-${arch}.${ext}
linux:
  target:
    - AppImage
    - deb
  maintainer: andresmarpz
  category: Utility
appImage:
  artifactName: ${name}-${version}-${arch}.${ext}
deb:
  artifactName: ${name}-${version}-${arch}.${ext}
npmRebuild: true
publish:
  provider: github
  owner: andresmarpz
  repo: sandcastle
  releaseType: draft
