node_modules
.turbo
.DS_Store
*.log

# Reference clones (not part of this project — see superset.sh integration work)
repos/

# Claude Code linked worktrees (each is its own full git worktree — never commit)
.claude/worktrees/
